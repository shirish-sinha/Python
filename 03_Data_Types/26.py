a = True
b = "True"

print(type(a))
print(type(b))

# a is of type bool because True is an actual Boolean value in Python.
# b is of type str because it is written inside quotes, which makes it
# just a sequence of characters rather than a real Boolean value.
